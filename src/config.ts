// to keep the code simple, have not used dotenv config
export const AuthConfig  = {
    JWT_PUBLIC_KEY_PATH: 'keys/jwt-token-public.key',
    JWT_REFRESH_TOKEN_PUBLIC_KEY_PATH: 'keys/refresh-token-public.key',
    JWT_TOKEN_PRIVATE_KEY_PATH: 'keys/jwt-token-private.key',
    LOGIN_TOKEN_EXPIRATION_TIME: '15m',
}